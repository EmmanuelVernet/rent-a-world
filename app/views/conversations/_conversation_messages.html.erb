<div class="flex-1 bg-base-100 flex flex-col">
  <!-- Chat Header -->
  <div class="navbar bg-base-200 min-h-16">
    <div class="navbar-start">
      <div class="flex items-center gap-3">
        <div class="avatar">
          <div class="w-10 rounded-full bg-primary text-primary-content flex items-center justify-center font-medium">
            <%= @sender.first_name[0] %><%= @sender.last_name[0] %>
          </div>
        </div>
        <div>
          <div class="font-medium"><%= @message.sender&.full_name %></div>
          <div class="text-sm opacity-60 flex items-center gap-1">
            <div class="badge badge-success badge-xs"></div>
            Online
          </div>
        </div>
      </div>
    </div>
    <div class="navbar-end">
      <div class="dropdown dropdown-end">
        <div tabindex="0" role="button" class="btn btn-ghost btn-sm btn-circle">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" class="w-5 h-5">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z" />
          </svg>
        </div>
        <ul tabindex="0" class="dropdown-content menu bg-base-100 rounded-box z-[1] w-52 p-2 shadow">
          <li><a>Profile</a></li>
          <li><a>Mute</a></li>
          <li><a>Block</a></li>
        </ul>
      </div>
    </div>
  </div>
  <!-- Messages Area -->
  <div class="flex-1 overflow-y-auto p-4 space-y-4 bg-base-50" 
  data-controller="messages" 
  data-messages-url-value="<%= conversation_messages_path(@conversation) %>" 
  data-messages-interval-value="3000">
    <!-- Received Message -->
    <div data-messages-target="list">
      <%= render "messages/messages", locals: { messages: @messages } %>
    </div>
    <!-- Typing Indicator -->
    <div class="chat chat-start">
      <div class="chat-image avatar">
        <div class="w-8 rounded-full bg-primary text-primary-content flex items-center justify-center text-sm font-medium">
          JD
        </div>
      </div>
      <div class="chat-bubble">
        <span class="loading loading-dots loading-sm"></span>
      </div>
    </div>
  </div>
  <!-- Message form input -->
  <%= render "messages/message_form", conversation: @conversation, message: @message %>
</div>